import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "DreamSage - AI Dream Interpretation",
  description: "Discover the meaning of your dreams through 71+ spiritual traditions with AI-powered analysis",
  keywords: "dream interpretation, spiritual guidance, AI analysis, dream meaning",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
